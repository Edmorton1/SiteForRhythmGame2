Контроллер
Контроллер создаёт сигналы.
Он должен создавать бесконечный поток при обращении к которому
происходит отправка сообщения по кафке. У него будет единственный Consumer

Главная проблема: Сделать получение сообщения обратно. Пока это решается
через создание map таблицы и id, которые содержат resolve.
Можно попробовать записывать в map какой-то колбек, который возвращает что-то по HTTP в будущем

-- СООБЩЕНИЕ ИДЁТ ЧЕРЕЗ JSON --

Главная проблема: Сделать так, чтобы Consumer понимал в какую именно функцию отправлять сигнал

Сервис
Сервис должен работать от Consumer, который при получении сигнала с сообщением
Парсит сообщение, получает данные к какой конкретно функции в сервисе нужно обратится
Внутри этой функции происходит отправка сообщения обратно

Пока всё буду писать в одном файле с хардкодом, чтобы было проще, без абстракций
Идея в том, чтобы сейчас придумать решение и перенести его на HTTP и абстракции